<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Upload Module</title>
    <style>
        body {
            margin: 0;
            padding: 10px;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            height: 100vh;
            max-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            box-sizing: border-box;
        }
        .upload-container {
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            flex: 1;
            overflow-y: auto;
            padding-right: 5px; /* Compensate for scroll bar */
        }
        /* Hide scrollbar for Chrome, Safari and Opera */
        .upload-container::-webkit-scrollbar {
            display: none;
        }
        /* Hide scrollbar for IE, Edge and Firefox */
        .upload-container {
            -ms-overflow-style: none;  /* IE and Edge */
            scrollbar-width: none;  /* Firefox */
        }
        .success-alert {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
            border-radius: 4px;
            padding: 10px 20px;
            font-size: 14px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            z-index: 1000;
        }
        .copy-id-btn {
            display: inline-block;
            background: #28a745;  /* Same green as confirm-btn */
            color: white;
            border: none;
            border-radius: 4px;
            padding: 3px 8px;
            font-size: 12px;
            margin-left: 8px;
            cursor: pointer;
            vertical-align: middle;
            transition: background-color 0.2s;
        }
        .copy-id-btn:hover {
            background: #218838;  /* Darker shade on hover */
        }
        .copy-id-btn:active {
            background: #1e7e34;  /* Even darker when clicked */
        }
    </style>
</head>
<body>
    <div id="statusMessage" class="status-message"></div>
    <div id="alertBox" class="success-alert" style="display: none;"></div>
</body>
</html>
